- name: Install Claude Code via npm on macOS
  command:
    cmd: "{{ asdf_home }}/shims/npm install --global @anthropic-ai/claude-code"
  environment:
    PATH: '{{ asdf_bin }}:{{ ansible_env.PATH }}'
