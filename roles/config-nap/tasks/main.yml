- name: Create local share if it doesn't exist
  file:
    mode: 0755
    path: "{{ local_share }}"
    state: directory
    recurse: yes

- name: Create symlink to nap directory 
  file:
    src: "{{ role_path }}/files"
    dest: "{{ local_share }}/nap"
    state: link
