# Ignore everything in `~/`
*

!/.config
!/.config/helix
!/.config/helix/*
!/.config/helix/themes
!/.config/helix/themes/*

# Track the file we are currently editing
!/.gitignore.d
!/.gitignore.d/helix